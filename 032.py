# Challenge 032

import math

radius = float(input("Enter the radius of a cylinder: "))
depth = float(input("Enter the depth of a cylinder: "))
volume = math.pi * (radius ** 2) * depth
print(f"Area: {volume:.3f}")